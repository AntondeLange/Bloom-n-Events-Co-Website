---
/**
 * Contact: hero with background image, two-column layout (contact info left, form right).
 * Matches contact.html layout and copy.
 */
import OptimizedImage from "../components/OptimizedImage.astro";
import BaseLayout from "../layouts/BaseLayout.astro";
import ContactForm from "../components/react/ContactForm";
import { CONTACT } from "../lib/constants";
import { getLocalBusinessSchema } from "../lib/structured-data";
import { buildSrcSet, encodeSrc, withWidthSuffix, DEFAULT_WIDTH } from "../lib/image-utils";

const localBusinessSchema = getLocalBusinessSchema();
const heroImage = "/assets/images/Brookton.JPG";
const heroImagePreloadSrc = encodeSrc(withWidthSuffix(heroImage, DEFAULT_WIDTH));
const heroImagePreloadSrcSet = buildSrcSet(heroImage);
const contactEmailHref = `mailto:${CONTACT.email}`;
const contactPhoneHref = `tel:${CONTACT.phone.replace(/\s/g, "")}`;
---

<BaseLayout
  title="Contact Bloom'n Events Co | Event Production Perth & WA"
  description="Contact Bloom'n Events Co for full-service event production in Perth & Western Australia. Phone: 1800 826 268. Email: enquiries@bloomneventsco.com.au. Based in Brookton, WA. Start the conversation today."
  path="/contact"
  lcpImageSrc={heroImagePreloadSrc}
  lcpImageSrcSet={heroImagePreloadSrcSet}
  lcpImageSizes="100vw"
>
  <!-- Contact Page with Hero Background -->
  <section class="contact-hero-section" aria-label="Contact">
    <div class="contact-hero-background">
      <OptimizedImage
        src={heroImage}
        class="contact-hero-image"
        alt="Brookton, WA - Home of Bloom'n Events Co"
        loading="eager"
        fetchpriority="high"
        decoding="async"
        width="1600"
        height="900"
        sizes="100vw"
      />
      <div class="contact-hero-overlay"></div>
    </div>

    <div class="container-fluid contact-hero-container">
      <div class="row g-0 h-100">
        <!-- Left Section - Contact Information -->
        <div class="col-12 col-lg-6 contact-info-section">
          <div class="contact-info-content">
            <h1 class="hero-headline">Let's Create Something Meaningful Together</h1>
            <p class="contact-description">
              Share your dates, budget, and the feeling you want to create. One team. One accountable partner. Full-service event production with engineering-grade care.
            </p>
            <p class="contact-info-text mt-3">
              <a href={contactEmailHref} class="contact-link">Email us</a>
              {" | "}
              <a href={contactPhoneHref} class="contact-link">Call {CONTACT.phone}</a>
            </p>

            <!-- Contact Info at Bottom Left -->
            <div class="contact-info-bottom">
              <div class="contact-info-grid">
                <!-- Location -->
                <div class="contact-info-block">
                  <h3 class="contact-info-heading">Location</h3>
                  <p class="contact-info-text">Bloom'n Events Co</p>
                  <p class="contact-info-text">Bloom'n Creative Design Co</p>
                  <p class="contact-info-text">Brookton, WA, 6306</p>
                  <p class="contact-info-text">Australia</p>
                </div>

                <!-- Social Media -->
                <div class="contact-info-block">
                  <h3 class="contact-info-heading">Social Media</h3>
                  <div class="social-media-grid">
                    <a
                      href="https://www.instagram.com/bloomneventsco/"
                      target="_blank"
                      rel="noopener noreferrer"
                      class="social-media-icon"
                      aria-label="Follow us on Instagram"
                    >
                      <i class="bi bi-instagram"></i>
                    </a>
                    <a
                      href="https://www.linkedin.com/company/bloom-n-events-co/"
                      target="_blank"
                      rel="noopener noreferrer"
                      class="social-media-icon"
                      aria-label="Follow us on LinkedIn"
                    >
                      <i class="bi bi-linkedin"></i>
                    </a>
                    <a
                      href="https://www.facebook.com/bloomneventsco/"
                      target="_blank"
                      rel="noopener noreferrer"
                      class="social-media-icon"
                      aria-label="Follow us on Facebook"
                    >
                      <i class="bi bi-facebook"></i>
                    </a>
                    <a
                      href="https://www.tiktok.com/@bloomneventsco"
                      target="_blank"
                      rel="noopener noreferrer"
                      class="social-media-icon"
                      aria-label="Follow us on TikTok"
                    >
                      <i class="bi bi-tiktok"></i>
                    </a>
                  </div>
                </div>

                <!-- Email -->
                <div class="contact-info-block">
                  <h3 class="contact-info-heading">Email</h3>
                  <p class="contact-info-text">
                    <a href={contactEmailHref} class="contact-link">{CONTACT.email}</a>
                  </p>
                </div>

                <!-- Contact -->
                <div class="contact-info-block">
                  <h3 class="contact-info-heading">Contact</h3>
                  <p class="contact-info-text">
                    <a href={contactPhoneHref} class="contact-link">{CONTACT.phone}</a>
                  </p>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Right Section - Contact Form Card -->
        <div class="col-12 col-lg-6 contact-form-section">
          <div class="contact-form-card">
            <div class="contact-form-header">
              <h2 class="contact-form-title">Tell Us About Your Vision</h2>
              <p class="contact-form-subtitle">Our team is ready to listen and guide every detail, big or small.</p>
            </div>

            <ContactForm client:load />
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Structured Data: LocalBusiness -->
  <script type="application/ld+json" is:inline set:html={JSON.stringify(localBusinessSchema)} />
</BaseLayout>
